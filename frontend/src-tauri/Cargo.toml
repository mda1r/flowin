[package]
name = "nexuspos"
version = "1.0.0"
description = "NexusPOS Desktop"
authors = ["NexusPOS Team"]
edition = "2021"

[lib]
name = "nexuspos_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[[bin]]
name = "nexuspos"
path = "src/main.rs"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-opener = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
panic = "abort"
codegen-units = 1
lto = true
opt-level = "s"
strip = true
